{
  "name": "near-contract-size-check-action",
  "version": "1.0.0",
  "private": true,
  "description": "GitHub Action to build NEAR WASM contracts, check size limits, compare with a baseline build, and suggest optimizations.",
  "type": "module",
  "main": "src/index.js",
  "scripts": {
    "test": "node --test"
  },
  "keywords": [
    "github-action",
    "near",
    "wasm",
    "size-check",
    "ci"
  ],
  "license": "MIT",
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "@actions/core": "^1.11.1",
    "@actions/exec": "^1.1.1"
  }
}
